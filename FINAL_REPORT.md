# ClinSynth — Final Report

**SH-405: Synthetic Patient Data Generation for Clinical Research**

---

## 1. What Was Built

ClinSynth: a complete, end-to-end privacy-preserving synthetic healthcare data platform with an 8-page Streamlit dashboard. The system loads patient data, trains a synthetic data generator, builds custom cohorts with demographic constraints, generates longitudinal health journeys, validates data statistically, screens for privacy leakage, and exports results.

## 2. Architecture

```
Data Ingestion -> Preprocessing -> SDV Synthesizer Training -> Sample Generation
  -> Cohort Conditioning (stratified resampling) -> Temporal Engine (learned distributions)
  -> Statistical Validation (KS, Wasserstein, TVD, correlation)
  -> Privacy Screening (duplicates, nearest-neighbor, near-copies)
  -> Streamlit Dashboard -> CSV/JSON/ZIP Export
```

Modular structure under `src/` with 9 subpackages: data, preprocessing, synthesis, cohort, temporal, validation, privacy, visualization, utils.

## 3. Dataset Used

Deterministic bundled demo dataset generated by `src/data/demo_generator.py`. All records are purely synthetic sample data with realistic clinical relationships built in (age-BP correlation, diabetes/hypertension subgroups, activity variation).

## 4. Dataset Size

- 500 demo patients (profiles)
- 15,000 longitudinal records (30 days x 500 patients)

## 5. Model Used

SDV library synthesizers:
- **CTGANSynthesizer**: Deep learning-based, learns complex relationships
- **GaussianCopulaSynthesizer**: Statistical copula-based, fast and stable

Both are fitted on the source dataset — they are not pretrained healthcare models.

## 6. Model Training Configuration

- CTGAN: configurable epochs (default 50 demo / 150 standard / 300 high quality), batch size 500
- Gaussian Copula: no epoch configuration needed
- Automatic fallback from CTGAN to Gaussian Copula on failure
- Model artifacts saved to `artifacts/models/`

## 7. Temporal Engine Implementation

Statistical temporal modeling that learns from source longitudinal data:
- Extracts baseline distributions and daily change distributions per variable
- Computes subgroup-specific parameters (diabetes/hypertension)
- Generates patient-specific trajectories with baselines influenced by demographics
- Applies day-to-day noise from learned distributions
- Supports configurable timelines (7-90 days)

## 8. Cohort Conditioning Implementation

Stratified resampling with constraint enforcement:
- Generates oversized candidate pool (5x requested)
- Stratifies by age (elderly vs young)
- Adjusts binary columns (diabetes, hypertension) via targeted flipping
- Iterates up to 20 attempts to minimize constraint deviation
- Reports requested vs actual percentages

## 9. Validation Metrics Implemented

- **Numerical**: mean, median, std, min/max, quantiles, KS test, Wasserstein distance
- **Categorical**: frequency comparison, total variation distance
- **Relationships**: correlation matrix comparison, mean absolute difference
- **Longitudinal**: daily mean trends, trend correlation
- **Overall**: fidelity score with documented formula

## 10. Privacy Metrics Implemented

- Exact duplicate detection (normalized comparison)
- Nearest-neighbor distance analysis (standardized Euclidean)
- Near-copy flagging (configurable threshold)
- Distance distribution visualization
- Overall screening status (PASSED / REVIEW NEEDED)

## 11. Tests Run

31 pytest tests across 8 test classes:
- TestDemoDataGeneration (7 tests)
- TestPreprocessing (3 tests)
- TestDatasetDetection (2 tests)
- TestCohortBuilder (4 tests)
- TestTemporalEngine (3 tests)
- TestValidation (5 tests)
- TestPrivacy (4 tests)
- TestExport (3 tests)

## 12. Test Results

**31 passed, 0 failed** (2 minor FutureWarnings from pandas/numpy edge cases with single-row test data).

## 13. Exact Run Command

```bash
pip install -r requirements.txt
streamlit run app.py
```

For tests:
```bash
pytest tests/ -v
```

## 14. Known Limitations

- Not clinically validated; demo/research platform only
- No HIPAA/GDPR compliance evaluation
- No formal differential privacy guarantees
- Demo data relationships are simulation assumptions
- CTGAN quality depends on training data size/diversity
- Python 3.14 used; Python 3.11+ recommended for widest SDV compatibility

## 15. Demo Walkthrough

1. Launch: `streamlit run app.py`
2. Data: Load demo dataset (500 patients, 15K longitudinal records)
3. Train: Select Gaussian Copula + Demo mode, train (~1 second)
4. Cohort: 5000 patients, 40% elderly, 30% diabetes, 25% hypertension, 30 days
5. Generate: ~30 seconds for 5000 patients + 150K longitudinal records
6. Validate: Review distribution comparisons, correlation heatmaps, trend charts
7. Privacy: Verify 0 exact duplicates, review nearest-neighbor distances
8. Export: Download CSV, JSON, or complete ZIP bundle

## 16. Important Files

| File | Purpose |
|------|---------|
| `app.py` | Main Streamlit application (8-page dashboard) |
| `src/data/demo_generator.py` | Deterministic demo dataset generator |
| `src/data/loader.py` | Dataset loading and schema mapping |
| `src/preprocessing/pipeline.py` | Data preprocessing and quality validation |
| `src/synthesis/synthesizer.py` | SDV synthesizer training/sampling |
| `src/cohort/builder.py` | Cohort conditioning engine |
| `src/temporal/generator.py` | Longitudinal health journey generator |
| `src/validation/engine.py` | Statistical validation metrics |
| `src/privacy/evaluator.py` | Privacy screening and evaluation |
| `src/visualization/charts.py` | Plotly chart components |
| `src/utils/config.py` | Centralized configuration |
| `src/utils/export.py` | CSV/JSON/ZIP export utilities |
| `tests/test_clinsynth.py` | pytest test suite (31 tests) |
| `requirements.txt` | Python dependencies |

## 17. Remaining Manual Steps

None. The application is fully functional end-to-end. All features work as specified.
